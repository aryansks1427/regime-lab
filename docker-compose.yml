version: '3.8'

services:
  regime-lab:
    build: .
    container_name: regime-lab-app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: python -m streamlit run app.py --server.port=8501 --server.address=0.0.0.0